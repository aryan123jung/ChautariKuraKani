import 'package:flutter/material.dart';

class ChautariScreen extends StatelessWidget {
  const ChautariScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Chautari screen"),
    );
  }
}